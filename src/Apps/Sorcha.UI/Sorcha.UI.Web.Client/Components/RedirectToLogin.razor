@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to login using relative path (works with /app/ base)
        Navigation.NavigateTo("auth/login", forceLoad: false);
    }
}
