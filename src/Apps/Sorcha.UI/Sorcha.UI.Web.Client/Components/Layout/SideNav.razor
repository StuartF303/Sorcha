@using Microsoft.AspNetCore.Components.Authorization

<div class="sidenav">
    <AuthorizeView>
        <Authorized>
            <nav class="sidenav-menu">
                <div class="sidenav-section">
                    <div class="sidenav-section-title">Main</div>
                    <NavLink class="sidenav-link" href="/" Match="NavLinkMatch.All">
                        <span class="sidenav-icon">🏠</span>
                        <span class="sidenav-text">Dashboard</span>
                    </NavLink>
                </div>

                <div class="sidenav-section">
                    <div class="sidenav-section-title">Blueprints</div>
                    <NavLink class="sidenav-link" href="/blueprints">
                        <span class="sidenav-icon">📋</span>
                        <span class="sidenav-text">My Blueprints</span>
                    </NavLink>
                    <NavLink class="sidenav-link" href="/blueprints/designer">
                        <span class="sidenav-icon">✏️</span>
                        <span class="sidenav-text">Designer</span>
                    </NavLink>
                </div>

                <div class="sidenav-section">
                    <div class="sidenav-section-title">Wallets</div>
                    <NavLink class="sidenav-link" href="/wallets">
                        <span class="sidenav-icon">👛</span>
                        <span class="sidenav-text">My Wallets</span>
                    </NavLink>
                </div>

                <div class="sidenav-section">
                    <div class="sidenav-section-title">Register</div>
                    <NavLink class="sidenav-link" href="/transactions">
                        <span class="sidenav-icon">📊</span>
                        <span class="sidenav-text">Transactions</span>
                    </NavLink>
                    <NavLink class="sidenav-link" href="/explorer">
                        <span class="sidenav-icon">🔍</span>
                        <span class="sidenav-text">Explorer</span>
                    </NavLink>
                </div>

                <div class="sidenav-section">
                    <div class="sidenav-section-title">Settings</div>
                    <NavLink class="sidenav-link" href="/settings">
                        <span class="sidenav-icon">⚙️</span>
                        <span class="sidenav-text">Settings</span>
                    </NavLink>
                </div>
            </nav>
        </Authorized>
        <NotAuthorized>
            <div class="sidenav-empty">
                <p class="text-muted">Please sign in to access features</p>
            </div>
        </NotAuthorized>
    </AuthorizeView>
</div>

@code {
    // Navigation component with authentication awareness
}
